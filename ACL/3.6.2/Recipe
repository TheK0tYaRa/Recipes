# Recipe for version 3.6.2 by TheK0tYaRa < thek0tyara.alod123@gmail.com >, on Sun 13 Apr 2025 05:45:38 PM EEST
# Recipe (MakeRecipe) for ACL by ro.ko@mcnon.com, on Tue Oct 12 00:32:04 BRST 2004
compile_version=017-GIT
url="https://github.com/acl-dev/acl/archive/refs/tags/v3.6.2.tar.gz"
file_size=26963986
file_md5=222b183fe58fb3a1fbd4860581a206b2
file="acl-3.6.2.tar.gz"
recipe_type=cmake #makefile #configure
#build_target="all_lib acl_master" #_lib"
dir='acl-3.6.2'
#configure_options=(
#   --libexecdir="$target/lib/acl"
#   --prefix="$target"
#)
cmake_options=(
#    --disable-static
    -DCMAKE_INSTALL_PREFIX=$target
    -DACL_BUILD_SHARED=YES
)
use_mysql=(
    -DHAS_MYSQL_DLL=YES
)
use_pgsql=(
    -DHAS_PGSQL_DLL=YES
)
use_sqlite=(
    -DHAS_SQLITE_DLL=YES
)
use_polarssl=(
    -DHAS_POLARSSL_DLL=YES
)
use_mbedtls=(
    -DHAS_MBEDTLS_DLL=YES
)
use_openssl=(
    -DHAS_OPENSSL_DLL=YES
)

#pre_build() {
#    find . -name 'Makefile*' -exec sed -Ei 's|^(\ +?CC\ +\=\ ?)g\+\+|\1clang++|g;s|^(\ +?CC\ +\=\ ?)gcc|\1clang|g' {} \;
#}
#install_variables=(
#   libdir="${target}/lib"
#   libexecdir="${target}/lib/acl"
#   PKG_DEVLIB_DIR="${target}/lib/acl"
#)

#install_target="install"
